//Angelyn Diaz

#include <iostream>

using namespace std; 

int main() {
    //Aqui se declara una variable
    int numero;

    // Se le solicta al usuario que ingrese un número
    cout<<"****Bienvenida personita****.\n";
    cout << "Ingresa un numero: ";
    cin >> numero;

    // Aqui se comprueba si el numero ingresado es par o impar
    if (numero % 2 == 0) {
        cout <<" Corecto "<< numero << " es un numero par.\n" << endl;
    } else {
        cout <<" Incorrecto " << numero << " no es un numero par.\n" << endl;
    }

    return 0;
}
